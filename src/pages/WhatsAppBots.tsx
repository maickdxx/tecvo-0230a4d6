import { AppLayout } from "@/components/layout";
import { BotManager } from "@/components/whatsapp/bots/BotManager";

export default function WhatsAppBots() {
  return (
    <AppLayout>
      <BotManager />
    </AppLayout>
  );
}
